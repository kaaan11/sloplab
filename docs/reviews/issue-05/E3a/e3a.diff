diff -- E3a-only changes vs delivery start snapshots (new files listed separately)
new file: src/sloplab/experiments/bundle.py (188 lines)
new file: tests/regression/test_bundle_completion.py (385 lines)
--- docs/reviews/issue-05/E3a/start/pilot.py	2026-09-10 01:23:58.523825660 +0300
+++ src/sloplab/experiments/pilot.py	2026-09-10 01:24:21.391200327 +0300
@@ -112,6 +112,7 @@
     records_path: Path
     manifest_path: Path
     outcomes_path: Path | None = None
+    completion_path: Path | None = None
     evaluations_attempted: int = 0
     failed_evaluations: int = 0
     skipped_by_budget: int = 0
@@ -657,11 +658,18 @@
     manifest_path = out_dir / "manifest.json"
     manifest_path.write_text(json.dumps(manifest, indent=2) + "\n", encoding="utf-8")
 
+    # Publish last: the completion marker covers records, outcomes, and the
+    # manifest, so the bundle never looks complete before all three exist.
+    from sloplab.experiments.bundle import write_completion
+
+    completion_path = write_completion(out_dir, kind="llm-pilot")
+
     return PilotRunResult(
         out_dir=out_dir,
         records_path=records_path,
         manifest_path=manifest_path,
         outcomes_path=outcomes_path,
+        completion_path=completion_path,
         evaluations_attempted=successful + failed,
         failed_evaluations=failed,
         skipped_by_budget=not_run,
--- docs/reviews/issue-05/E3a/start/main.py	2026-09-10 01:23:58.524710339 +0300
+++ src/sloplab/cli/main.py	2026-09-10 01:24:25.619198311 +0300
@@ -409,6 +409,12 @@
     _write_comparison_markdown(
         out_dir / "report.md", bundles, comparisons, cis, taxonomy_counts, result.experiment_name
     )
+    # Publish last: the completion marker covers the runner files plus the
+    # analysis, CSV, and report above, so the study never looks complete
+    # before every required file and hash is in place.
+    from sloplab.experiments.bundle import write_completion
+
+    write_completion(out_dir, kind="study")
     click.echo(f"study complete -> {out_dir} ({result.case_count} cases, {len(names)} evaluators)")
 
 
