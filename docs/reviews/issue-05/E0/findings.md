# E0 kritik bulgu matrisi

Durum sözlüğü: `DOĞRULANDI` / `GİDERİLMİŞ` / `ÇÜRÜTÜLDÜ` / `BU_GİRİŞTE_ERİŞİLEMEZ` / `BELİRSİZ`.
Kanıt türü ayrıca `kod` / `kayıtlı artefakt` / `bu tur çalıştırıldı` olarak yazıldı.
Kod mekanizması kanıtı, üretimde gerçekleştiğinin kanıtı değildir; erişilebilirlik
sütunu bu ayrımı taşır. Hiçbir satır sessizce kapatılmadı.

## Zorunlu satırlar

| ID | İddia | Güncel `dosya:satır` | Kanıt türü | Durum | Erişilebilirlik / artefakt etkisi | Sonraki faz |
| --- | --- | --- | --- | --- | --- | --- |
| B1 | Çok satırlı numaralı adımın yalnız ilk satırı silinir, devam satırı türevde kalır | `src/sloplab/mutations/operators/evidence.py:60-67`; `src/sloplab/mutations/textops.py:45-53`; `src/sloplab/corpus/parser.py:32-42` | kod + kayıtlı artefakt | DOĞRULANDI | Üretimde gerçekleşti: `totiming-019` (`corpus/canonical/totiming-019/report.md:27-30` → `study-v02/adversarial/totiming-019/mut-totiming-019-remove-reproduction-step-02/report.md:25-29`, `interleaved to cancel drift.` tek başına), `oauthstate-029` (`corpus/.../report.md:25-28` → `.../mut-oauthstate-029-remove-reproduction-step-02/report.md:25-27`, `code parameter.` kalıntısı), `saml-019` (`corpus/.../report.md:26-29` → `.../mut-saml-019-remove-reproduction-step-03/report.md:26-28`, valid ebeveynde aynı mekanizma). Silici mekanizma güncel kodda değişmeden sürüyor. Üç örnek toplam etki sayısı değildir | E5 (tam etki envanteri + G4 biçim matrisi) |
| B8/U1 | Hash'lenen prompt ile gönderilen prompt aynı değil | `src/sloplab/evaluators/llm/adapter.py:39-62,110`; `src/sloplab/experiments/pilot.py:249`; `experiments/prompts/triage-v1.md:1-38` | kod | DOĞRULANDI | Adapter `PROMPT_TEMPLATE` sabitini gönderir; pilot `config.prompt_file` dosyasını hash'ler; iki metin aynı değil. Canlı istek yapılmadı; güncel artefakt etkisi yok (offline kayıtlarda prompt_hash null). Doğrudan `.format()` ile dosya takmak güvenli geçiş değildir (JSON süslü parantezleri) | E1/G2 (çözülmüş tarif + render testi) |
| B8 | `_failed_result`/karşılığı karar üretir, metrik tüketicisi dışlamaz | `src/sloplab/evaluators/llm/adapter.py:201-222`; `src/sloplab/models/run.py:81-98`; `src/sloplab/scoring/metrics.py:53-54,219-255`; `src/sloplab/scoring/comparison.py:48-67,210-231` | kod | DOĞRULANDI | `_failed_result` `needs_manual_review` + confidence `0.0` + beş `0.5` boyut üretir (`failed=True` metadata ile). `correct`, beklenen karar review ise `True` olur; hiçbir metrik/karşılaştırma `failed` dışlaması yapmaz. Offline evaluator'larda bu yol tetiklenmez; canlı pilot kayıtlarını zehirleyebilir | E2/G3 (typed outcome + payda ayrımı) |
| B8 | Fiziksel cap ve retry sahipliği; script vs doğrudan pilot ayrımı | `src/sloplab/experiments/pilot.py:29-54,69-140,172-190`; `src/sloplab/evaluators/llm/adapter.py:113-124`; `scripts/llm_bench.py:68-84,111-123` | kod | DOĞRULANDI | `CountingClient` fiziksel cap'i, `ThrottledClient` aralık/429'u uygular; retry adapter'dadır. Parse/transport/bütçe hatası aynı attempt döngüsündedir; bütçe tükenmesi (`BudgetExhausted`) genel exception koluna girer. `effective_cap=min(config,client)` her iki yolda da var; worst-case preflight reddi yalnız script yolundadır (`llm_bench.py:80-98`), doğrudan pilot çağrısında yok. Script varsayılanında iki cap aynı kurulur, ayrım orada etkin değil | E2/G3 (tek koordinatör, hata sınıfı ayrımı) |
| B8/U2/U9 | Severity dönüşümü; türev kind/dimensions aktarımı; full config vaka seçimi | `src/sloplab/evaluators/llm/adapter.py:168-180`; `src/sloplab/models/enums.py:46-51`; `src/sloplab/experiments/pilot.py:170,204-215`; `scripts/llm_bench.py:125-133`; `experiments/configs/llm-full-v0.2.yaml:8-18` | kod + bu tur çalıştırıldı (enum/ECE mikro kontrolü) | DOĞRULANDI | Severity: `severity in Severity.__members__` üye **adlarını** (BÜYÜK HARF) sınar; normal `"high"` değertutmaz → `MEDIUM`'a düşer; `"HIGH"` ise `Severity("HIGH")` değer aramasında `ValueError` verir (bu tur çalıştırıldı, `logs/microchecks.stdout.log`). Pilot türev verilse bile `case_kind="canonical"` sabit yazar ve `expected_dimensions` aktarmaz. `llm-full` "canonical+derived" açıklar ama script yalnız canonical filtreler. Üçü de canlı-yol örtük kusuru; offline kayıtlara etkisi yok | E2/G3 (severity testi, kayıt aktarımı, seçim testi) |
| B10/U4 | Susceptibility parent/child ağırlıkları; sıfır-davranış karşıörneği | `src/sloplab/scoring/metrics.py:124-152` | kod + kayıtlı artefakt | DOĞRULANDI | Formül: türev kabul oranı − benzersiz ebeveyn kabul oranı; parent `i` için `Σmᵢaᵢ/Σmᵢ − Σaᵢ/P`. Tüm child kararları parent ile aynı olsa bile genel olarak sıfır değildir (ağırlık değişimini de ölçer). Kayıtlı sinyal: `study-v02/analysis.json` evidence-graph `drift 0.0`, `susceptibility +0.006787330316742085`. Tam ağırlık-payının paired yeniden hesaplaması G5'e kaldı | E4/G5 (eşleştirilmiş ağırlık + bağımsız yeniden hesaplama) |
| B10/U3 | ECE son bini: `1.0` ile `[0.9,1.0)` birlikteyken davranış; mevcut test kapsamı | `src/sloplab/scoring/metrics.py:155-180`; `tests/regression/test_calibration_binning.py:60-63` | kod + bu tur çalıştırıldı | DOĞRULANDI | Son bin yalnız boşsa sağdan kapatılır (`if b==last and not members`); karışık durumda `1.0` dışarıda kalır ama payda tüm kayıtları içerir. Bu tur çalıştırıldı: `0.95/doğru + 1.0/yanlış` → ECE `0.025` (1.0 düşmüş); iki kayıt da sayılsaydı `|0.975−0.5|=0.475` olurdu. Mevcut `0.0/yanlış + 1.0/doğru` testi yalnız boş-bin durumunu sınar, karışık durumu kapsamaz. Yayınlanan iki baseline `1.0` üretmez (o kayıtların ECE'sinin yanlış olduğunun kanıtı değil); LLM yolu için gerçek açık | E4/G5 (karışık-bin testi) |
| B10 | Eksik repeat unanimous sayılabilir; beklenen repeat sayısı kontrolü yok | `src/sloplab/scoring/comparison.py:210-232` | kod | DOĞRULANDI | `repeat_stability` vaka başına gözlemleri birleşimde toplar; tek gözlemli vaka `len(decisions)==1` ile unanimous sayılır; planlanan repeat sayısı parametre olarak alınmaz. Pilot `len(repeat_sets)>=2 ve hepsi dolu` ise hesaplar (`pilot.py:223-227`), eksik-tekrar kapsamı denetlenmez | E4/G5 (coverage + tekrar kimliği) |
| B11 | `correct` farklı tüketiciler; `compare` gerçek girişleri | `src/sloplab/scoring/metrics.py:53-54`; `src/sloplab/scoring/comparison.py:48-67,158-179`; `src/sloplab/models/run.py:81-99`; `src/sloplab/cli/main.py:450-491` | kod | DOĞRULANDI | `compute_metrics` doğruluğu yeniden hesaplar; bootstrap/paired saklanan `correct` alanını kullanır (alan değişirse iki gerçek ayrışır). CLI `compare` `run.jsonl`/`records.jsonl` okumaz; dizindeki `metrics-*.json` dosyalarını okur; study çıktısında `metrics-*.json` üretilmez (study `analysis.json` üretir), kayıt-cache bağı bağlanmaz | E3/G6 + E4 (okuyucu denetimi, cache bağı) |
| B11/U11 | Writer/reader tamamlanma sınırı; safety-blocked görünürlüğü | `src/sloplab/reporting/writers.py:15-21`; `src/sloplab/experiments/runner.py:103-118`; `src/sloplab/cli/main.py:195-215,346-401`; `src/sloplab/mutations/materialize.py:127-145`; `src/sloplab/experiments/study.py:64-78` | kod | DOĞRULANDI | Doğrudan yazım; manifesto records'tan önce, CLI metrikleri kayıttan önce yazılır; tek tamamlanma sınırı/commit işareti yok; okuyucular (`read_run_jsonl`, `read_suite_index`) şema doğrular, bütünlük reddi yapmaz. Materializer safety-violation'da planı yazar-dizine eklemez, ihlali yalnız dönüş değerinde taşır; study sonucu incelemeden devam eder → safety-blocked planlar bundle'da görünmez. Kesinti olmuş gibi iddia edilmiyor; gerçekleşmiş kısmi-bundle olayı gösterilmedi | E3/G6 (atomik yayın + kesinti testleri) |
| B12 | Study/generation seed ayrımı; uygulanmadan hash'lenen evaluator ayarları | `src/sloplab/experiments/study.py:64-100`; `src/sloplab/experiments/config.py:19-48`; `benchmarks/suites/v1-core.yaml:8`; `experiments/configs/deterministic-study-v0.2.yaml:19` | kod | DOĞRULANDI | Study seed'i materializer'a verilmez; üretim suite `base_seed`'inden, kaydedilen study seed'i study config'inden gelir (bugünkü dosyalarda ikisi de `20260825`, sınır görünmez). `EvaluatorSpec.config` hash'lenir (`study.py:89`) ama `get_evaluator(spec.name)` config'i uygulamaz (`study.py:84`); analysis/provenance seçeneklerinin bir bölümü yürütme dallarında etkili değil. Mevcut boş/default config'ler bugün yanlış sayı üretmiş sayılmaz; API esnekliği gerçek değil | E1/G2 (çözülmüş tarif, unsupported reddi) |
| B3/B12/B8-red | Raporda reddedilen `:02d`, smoke fallback, CLI `max_cases=None` iddiaları | `src/sloplab/mutations/planner.py:38-41`; `src/sloplab/models/suite.py:57-66`; `benchmarks/configs/smoke.yaml:3-24`; `scripts/llm_bench.py:68-84`; `src/sloplab/experiments/pilot.py:170` | kod | DOĞRULANDI (retler geçerli) | `:02d` minimum genişliktir, 100'de taşmaz/çakışmaz. `smoke.yaml`'da `presentation_pair` yoksa sınıf politikasına düşülür ve smoke gerekli sınıfları içerir (uncovered-class iddiası çürük); buna karşılık "first 12" seçimini gerçekleştiren alan/yol yok. CLI'da pozitif değer doğrulanıp atanır, `None` erişilemez (`max_cases=None` yalnız direkt pilot/full-config yolunda anlamlı: pilot `None`'u "tümü" sayar) | E1 (desteklenmeyen kombinasyon envanteri) |

## Kalan B maddeleri (kısa durum)

| ID | Durum | Dayanak ve sınır |
| --- | --- | --- |
| B2 | DOĞRULANDI (garanti açığı; fiilî ayrışma gözlenmedi) | kod: `src/sloplab/mutations/base.py:78-81`, `src/sloplab/mutations/materialize.py:124`, `src/sloplab/scoring/comparison.py:168-173`; `.github/workflows/ci.yml:10-28` (3.11-3.13, ortam-içi eşitlik; çapraz-ortam karşılaştırması yok); `docs/reproducibility.md:3-4` vs `:60-64`. Bu fazda çapraz Python/kök koşusu yapılmadı → E6 |
| B3 (kalan) | DOĞRULANDI | kod: `src/sloplab/mutations/planner.py:44-62,94-118`, `src/sloplab/corpus/loader.py:158-178`, `src/sloplab/models/suite.py:57-66`. Kayma aynı politika grubundaki sonraki fixture'lar içindir; sabit korpusta nondeterminizm değildir; içerik bağı (index) yok → E1 |
| B4 | DOĞRULANDI (kapsam-dışı tasarım; mevcut üretim darboğazı değil) | kod: `src/sloplab/models/manifest.py:94-104`, `src/sloplab/mutations/base.py:24-41`, `src/sloplab/mutations/planner.py:49-79`, `src/sloplab/mutations/materialize.py:119-125`, `src/sloplab/mutations/operators/impact.py:138-158`. Tek-adım sınırı korunmalı; zincirleme uygulanmıyor → E5 sınırı |
| B5 | DOĞRULANDI (veri akışı + blast radius; insan-ölçek uyumu snapshot'tan çıkmaz) | kod: `src/sloplab/mutations/base.py:84-91`, `src/sloplab/scoring/metrics.py:183-193`, `src/sloplab/models/manifest.py:31-52,109-119`. Yalnız delta değişimi decision accuracy/MDR/FRR/ECE'yi değiştirmez; `0.5` varsayılanı fiilî kullanım envanteri ayrı iş → E4 |
| B6 | DOĞRULANDI (mekanizma + yorum sınırı) | kod: `src/sloplab/evaluators/rules/baseline.py:136-144,216-230,260-275,314-316`; kayıtlı ECE `study-v02/analysis.json` rules `0.2977…`, graph `0.2701…`. `[:1]` yalnız finding listelerini kısaltır; `len()` cezaları tam listeleri kullanır (izlenebilirlik açığı, kesilme hatası değil). `0.58→0.81` tek-kelime nedensel etki gibi okunamaz → E4 (rol dondurma) |
| B7 | DOĞRULANDI (H8 çatışması çürük; kontrol rolü korunmalı) | kod: `src/sloplab/evaluators/rules/evidence_graph.py:129-149,214-221`; test: `tests/unit/test_evidence_graph_families.py:75-114`. Graf içerik sinyallerine tepkili; testler tepki + değişmezlik yükümlülüklerini ayrı tanımlar → E4 (sürüm dondurma, ayrı evaluator kimliği) |
| B9 | DOĞRULANDI (tekrar-okuma yapısal; süre darboğazı ölçülmedi) | kod: `src/sloplab/scoring/harness.py:103-165`, `src/sloplab/experiments/study.py:83-91`, `src/sloplab/evaluators/base.py:30-55`. `D×(1+E)` çağrı yapısı + etiket/metnin farklı zamanda alınması; `run_case` exception yakalamaz. Paralellik sırası config sırası olmalı (alfabetik değil); profil G7'ye kaldı → E1/E2 + E6 |

## Kalan U maddeleri (kısa durum)

| ID | Durum | Dayanak ve sınır |
| --- | --- | --- |
| U5 | DOĞRULANDI (metadata/tutarlılık) | kod: `src/sloplab/mutations/base.py:33-36`, `src/sloplab/mutations/operators/presentation.py:178-190`. `confidence_overstatement` boyut deltaları negatif ama `claim_quality` default `"unchanged"` kalır → E4 uygunluk tablosu |
| U6 | DOĞRULANDI (mekanizma; hile kanıtı değil) | kod: `src/sloplab/scoring/harness.py:141-148` (etiket her evaluatora verilir), `src/sloplab/models/evaluation.py:51-63`, `docs/evaluator-contract.md:21-29`; built-in'ler etiketi yoksayar (`adapter.py:109`, `evidence_graph.py:100`, `baseline.py:179`) ve labels-stripped birim testi var → E1/E2 |
| U7 | DOĞRULANDI (mekanizma) / BELİRSİZ (korpus etkisi) | kod: `src/sloplab/evaluators/rules/baseline.py:236-241` — tek `any(conditional)` tüm unconditional'ları bastırır. Mevcut korpus etkisi ölçülmedi → E4 |
| U8 | DOĞRULANDI (mekanizma) / BELİRSİZ (sonuç etkisi) | kod: `src/sloplab/corpus/parser.py:32-42` (bölüm metni başlığı içerir), `src/sloplab/models/report.py:49-55`, `src/sloplab/evaluators/rules/evidence_graph.py:116-136` (başlık tek başına node doldurabilir). Mevcut sonuç etkisi gösterilmedi → E4 |
| U10 | DOĞRULANDI (kayıtlı artefakt) / BU_GİRİŞTE_ERİŞİLEMEZ (export tarafı) | kayıtlı artefakt: `study-v02/manifest.json:3` `14547a0…`; bu tur: commit nesnesi yerelde var, HEAD'in atası, `v0.3.0` tag'i HEAD'i (`9960f4f`) gösterir. Export `SNAPSHOT.txt` repo dışındadır, bu girişte doğrulanamaz |
| U12 | DOĞRULANDI | kod: `src/sloplab/mutations/materialize.py:79-91`, `src/sloplab/scoring/harness.py:111-119`, `src/sloplab/scoring/comparison.py:92-97`. `per_class` etiketi parent kökenini taşır; mutated-valid başarı gibi okunamaz → E4 |

## Reddedilen teşhislerin durumu

`:02d` taşması, smoke uncovered-class politikası ve CLI `max_cases=None` bütçe
atlaması iddiaları güncel kodda da geçersizdir (yukarıdaki B3/B12/B8-red satırı).
Bunlar iş listesine alınmamalı; E1 yalnız gerçekten desteklenmeyen kombinasyonları
envanterler.
